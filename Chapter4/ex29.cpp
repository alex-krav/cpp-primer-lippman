#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main_ex29()
{
	//int x[10], * p = x;
	//cout << sizeof(x) / sizeof(*x) << endl;
	//cout << sizeof(p) / sizeof(*p) << endl;

	return 0;
}