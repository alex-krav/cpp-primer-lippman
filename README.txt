C++ QA:
- lvalue vs rvalue
- static_cast vs reinterpret_cast - ex 4.37, page 227

Answers:
- left-associative 
	((cout << "hi") << "there") << endl;
- right-associative
	(grade > 95) ? "A" : ((grade > 85) ? "B" : ((grade > 75) ? "C" : ("D")));
- const_cast "casts away the CONST"

After Lippman:
- chapter2: variable types and converstion between 


	
