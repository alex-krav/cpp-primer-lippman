#include <iostream>

using namespace std;

//int f();

int main_decltype()
{
	//decltype(f()) sum;
	//
	//const int ci = 0, & cj = ci;
	//decltype(ci) x = 0;
	//decltype(cj) y = x;
	//decltype(cj) z;

	//int i = 42, * p = &i, & r = i;
	//decltype(r + 0) b; //int
	//decltype(*p) c; //int&

	//decltype(i) e; //int
	//decltype((i)) d; //int&

	return 0;
}